# Agent Workflows

## Overview

SignalForge War Room uses deterministic tools first, then AI agents, then validation.

The system is intentionally CLI-first and UI-optional.

## Workflow 1: Data Validation

Command:

```bash
pnpm sf data:check